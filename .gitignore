# Dependencies
node_modules/

# Build outputs
dist/
build/
.output/
*.bun-build
*.tsbuildinfo

# Bun
.bun/
bun.lockb

# Lockfiles non canoniques — repo PM-agnostique, canonique = bun.lock.
# Chaque dev peut utiliser npm/pnpm/yarn ; son lockfile reste local.
pnpm-lock.yaml
package-lock.json
yarn.lock

# Environment
# Ce qui est propre à la MACHINE (ADR-0065). Les défauts de développement, eux, sont versionnés :
# `.env.echoppe` et `.env.prisme` sont suivis par git et ne portent aucun secret.
#
# `.env` reste ignoré : c'est le fichier du poste, lu par Docker Compose seul.
.env
.env.local
# Les surcharges de poste, un `.local` par produit. `.env.*.local` les couvre toutes, y compris
# celles d'un produit qui n'existe pas encore.
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*

# Test
coverage/

# Drizzle
drizzle/meta/

# Temporary
tmp/
.temp/

# VitePress
docs/.vitepress/cache/
docs/.vitepress/dist/

# Uploads
/apps/echoppe-api/uploads/

# Claude Code — réglages personnels (le CLAUDE.md partagé reste versionné)
.claude/settings.local.json
